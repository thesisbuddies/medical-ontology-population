import java.io.IOException;
import java.util.ArrayList;

import thsst.ontopop.validation.model.ArticleModel;
import thsst.ontopop.validation.model.ConditionModel;


public class OntologyPopulationTest {
	public static void main(String[] args){
		//OntologyPopulation op = new OntologyPopulation();
		
		//op.CreateOntology();
		
		/*ArticleModel aModel = new ArticleModel(null, "");
		
		aModel.setConditionName("Scurvy");
		
		ArrayList<String> aList = new ArrayList<String>();
		aList.add("Orange");
		aModel.setAvoidFood(aList);
		
		aList.add("Orange");
		aModel.setNeededFood(aList);
		
		aList.add("Orange");
		aModel.setAvoidNutrients(aList);
		
		aList.add("Orange");
		aModel.setNeededNutrients(aList);
		
		aList.add("Orange");
		aModel.setSymptoms(aList);
		
		aList.add("Orange");
		aModel.setSynonyms(aList);
		
		
		try {
			op.AddToOntology(aModel);
		} catch (IOException e) {
			// TODO Auto-generated catch block
			e.printStackTrace();
		}
		*/
		/*op.getAllConditions();
		ConditionModel cm = op.getCondition("Rickets");
		
		System.out.println(cm.getConditionName());
		
		for(String s: cm.getSymptoms()){
			System.out.println(s);
		}*/
		
	}
}
